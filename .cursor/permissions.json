{
  "autoRun": {
    "allow_instructions": [
      "プロジェクト内を対象とした操作は許可する",
      "作業ブランチに対する `git add` / `git commit` / `git push` は許可する（`develop` への直接コミットや force push は除く）"
    ],
    "block_instructions": [
      "プロジェクト外のファイルを変更する操作は許可しない",
      "git管理していないファイルを変更する操作は許可しない",
      "gitの履歴を書き換える操作は許可しない",
      "権限の変更は許可しない",
      "本番環境へのアクセスは許可しない"
    ]
  }
}
